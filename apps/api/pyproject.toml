[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
authors = [{ name = "Greg Brown", email = "greg@domainfusion.io" }]
dependencies = []

[project.scripts]
api = "api:main"

[dependency-groups]
dev = [
  "pytest>=9.0.3",
  "ruff>=0.15.15",
  "ty>=0.0.40",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
